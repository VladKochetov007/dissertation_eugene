# Core graph analysis
networkx>=3.0

# GNN-TCN model (uses pure PyTorch, no torch-geometric dependency)
torch>=2.0

# Scientific computing (used by gnn/dataset.py, gnn/graph_builder.py)
numpy>=1.24

# Data collection (collectors use only stdlib urllib — no extra deps)

# Testing
pytest>=7.0

# Optional: torch-geometric (uncomment if switching GAT to PyG implementation)
# torch-geometric>=2.4

# Optional: ClickHouse bridge (uncomment if using)
# clickhouse-connect>=0.7

# Optional: Causal inference (uncomment if extending)
# dowhy>=0.11
# causal-learn>=0.1
